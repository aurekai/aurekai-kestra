name: validate

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  flows:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate flow shape
        run: bash tests/validate-flows.sh
