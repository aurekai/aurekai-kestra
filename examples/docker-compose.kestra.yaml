version: "3.8"
services:
  kestra:
    image: kestra/kestra:latest
    command: server local
    ports:
      - "8080:8080"
    environment:
      KESTRA_CONFIGURATION: |
        kestra:
          server:
            basic-auth:
              enabled: false
